{
  "name": "mondabot-dashboard-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:next\"",
    "dev:next": "cd mondabot-dashboard && npm run dev",
    "dev:server": "cd server && npm run dev",
    "server": "cd server && npm start",
    "build": "cd mondabot-dashboard && npm run build",
    "check-server": "node scripts/check-server.js",
    "setup": "npm install && cd server && npm install && cd ../mondabot-dashboard && npm install",
    "check-env": "node -r dotenv/config -e \"require('dotenv').config({ path: 'server/.env' }); console.log('Checking environment...'); const required = ['AIRTABLE_API_KEY', 'AIRTABLE_BASE_ID']; const missing = required.filter(v => !process.env[v]); if (missing.length) { console.error('❌ Missing required env vars:', missing.join(', ')); console.error('📄 Please check your .env file in the server directory'); process.exit(1); } else { console.log('✅ All required environment variables are set'); }\"",
    "health": "npm run check-env && npm run check-server && echo '🎉 All systems are healthy!'",
    "clean": "cd mondabot-dashboard && rm -rf .next out node_modules && cd ../server && rm -rf node_modules",
    "fresh-install": "npm run clean && npm run setup"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^6.7.2",
    "dotenv": "^17.0.0",
    "lucide-react": "^0.522.0",
    "swr": "^2.3.3"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
