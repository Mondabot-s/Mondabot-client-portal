{
  "name": "mondabot-dashboard-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"🔧BACKEND,🎨FRONTEND\" --prefix-colors \"blue,green\" \"npm run backend\" \"npm run frontend\"",
    "backend": "cd server && npm run dev",
    "frontend": "cd mondabot-dashboard && npm run dev",
    "dev:server": "cd server && npm run dev",
    "dev:next": "cd mondabot-dashboard && npm run dev",
    "dev:windows": "powershell -ExecutionPolicy Bypass -File scripts/start-dev.ps1",
    "dev:windows:force": "powershell -ExecutionPolicy Bypass -File scripts/start-dev.ps1 -Force",
    "dev:windows:clean": "powershell -ExecutionPolicy Bypass -File scripts/start-dev.ps1 -Clean",
    "server": "cd server && npm start",
    "build": "cd mondabot-dashboard && npm run build",
    "start": "cd mondabot-dashboard && npm start",
    "clean": "rm -rf .next && rm -rf node_modules/.cache && cd mondabot-dashboard && rm -rf .next out node_modules && cd ../server && rm -rf node_modules",
    "reset": "npm run clean && npm install",
    "health-check": "powershell \"Invoke-WebRequest -Uri http://localhost:3001/health -Method Get; Invoke-WebRequest -Uri http://localhost:3000 -Method Get\"",
    "check-server": "node scripts/check-server.js",
    "setup": "npm install && cd server && npm install && cd ../mondabot-dashboard && npm install",
    "check-env": "node -r dotenv/config -e \"require('dotenv').config({ path: 'server/.env' }); console.log('Checking environment...'); const required = ['AIRTABLE_API_KEY', 'AIRTABLE_BASE_ID']; const missing = required.filter(v => !process.env[v]); if (missing.length) { console.error('❌ Missing required env vars:', missing.join(', ')); console.error('📄 Please check your .env file in the server directory'); process.exit(1); } else { console.log('✅ All required environment variables are set'); }\"",
    "health": "npm run check-env && npm run check-server && echo '🎉 All systems are healthy!'",
    "fresh-install": "npm run clean && npm run setup",
    "check-ports": "powershell \"netstat -ano | findstr :3000; netstat -ano | findstr :3001\"",
    "test-production": "set NODE_ENV=production && npm run railway:start",
    "test-production:unix": "NODE_ENV=production npm run railway:start",
    "test-railway": "node scripts/test-railway-build.js",
    "railway:build": "npm install && npm run build && cd server && npm install",
    "railway:start": "concurrently --names \"🚀API,🌐WEB\" --prefix-colors \"blue,green\" \"npm run start:api\" \"npm run start:web\"",
    "start:api": "cd server && npm start",
    "start:web": "cd mondabot-dashboard && npm start",
    "start:server": "cd server && npm start"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^6.7.2",
    "dotenv": "^17.0.0",
    "lucide-react": "^0.522.0",
    "swr": "^2.3.3"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "wait-on": "^7.2.0"
  }
}
